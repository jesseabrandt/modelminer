% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula_wrapper.R
\name{formula_wrap}
\alias{formula_wrap}
\title{Wrap a model function to accept a formula interface}
\usage{
formula_wrap(model_func, x_name = "x", y_name = "y")
}
\arguments{
\item{model_func}{A model function that accepts a predictor matrix and
response vector as named arguments.}

\item{x_name}{Name of the predictor matrix argument in \code{model_func}.
Defaults to \code{"x"}.}

\item{y_name}{Name of the response vector argument in \code{model_func}.
Defaults to \code{"y"}.}
}
\value{
A new function with the signature \code{function(formula, data, ...)}
  that extracts \code{x} and \code{y} from \code{data} using the formula and
  calls the original \code{model_func}.
}
\description{
Adapts functions that take a predictor matrix \code{x} and response vector
\code{y} (e.g. \code{glmnet}) into functions that accept a \code{formula}
and \code{data} argument, making them compatible with \code{mine()}.
}
\examples{
\dontrun{
formula_glmnet <- formula_wrap(glmnet::glmnet)
result <- mine(mtcars, mpg, model_func = formula_glmnet)
}

}
