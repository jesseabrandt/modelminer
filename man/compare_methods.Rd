% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_methods.R
\name{compare_methods}
\alias{compare_methods}
\title{Run and compare multiple mine() configurations side-by-side}
\usage{
compare_methods(data, response_var, configs, ...)
}
\arguments{
\item{data}{A data frame containing the response and predictor variables.}

\item{response_var}{The name of the response variable (unquoted).}

\item{configs}{A named list of argument lists. Each element is passed to
\code{mine()} as overrides on top of the shared \code{...} defaults.
\code{method} may be a string or a custom search function in any config.}

\item{...}{Default \code{mine()} arguments shared by all configs. Values
in individual \code{configs} entries take precedence.}
}
\value{
A list with two elements:
  \describe{
    \item{\code{summary}}{A data frame with one row per config: Config,
      Formula, BestMetric (numeric; NA if non-numeric), and
      ModelsEvaluated.}
    \item{\code{details}}{A named list of the full \code{mine()} results.
      Each element contains \code{Formula}, \code{all_models}, \code{model},
      \code{best_metric}, and \code{method}.}
  }
}
\description{
A convenience wrapper around \code{\link{mine}} for experimenting with
different search algorithms, candidate-pool settings, or custom ("draft")
search functions without having to manually re-run and collect results.
}
\examples{
\donttest{
# Compare greedy vs forward_backward with different polynomial degrees
cmp <- compare_methods(
  mtcars, mpg,
  configs = list(
    greedy_d1 = list(method = "greedy",          max_degree = 1),
    greedy_d3 = list(method = "greedy",          max_degree = 3),
    fwd_bwd   = list(method = "forward_backward", max_degree = 1)
  ),
  max_interact_vars = 1
)
cmp$summary

# Compare with different interaction settings
cmp2 <- compare_methods(
  mtcars, mpg,
  configs = list(
    no_interact = list(method = "greedy", max_interact_vars = 1),
    interact    = list(method = "greedy", max_interact_vars = 2)
  ),
  max_degree = 1, verbose = FALSE
)
}
}
